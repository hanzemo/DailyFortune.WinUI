<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="DailyFortune.WinUI.Views.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <StackPanel Width="360" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16">
            <TextBlock Text="每日运势" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Text="登录你的账号" HorizontalAlignment="Center"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            <TextBox Header="用户名" Text="{x:Bind ViewModel.Username, Mode=TwoWay}"/>
            <PasswordBox Header="密码" Password="{x:Bind ViewModel.Password, Mode=TwoWay}"/>
            <TextBlock Text="{x:Bind ViewModel.Error, Mode=OneWay}"
                       Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                       TextWrapping="Wrap"/>
            <Button Content="登录" HorizontalAlignment="Stretch"
                    Style="{StaticResource AccentButtonStyle}"
                    Command="{x:Bind ViewModel.LoginCommand}"
                    IsEnabled="{x:Bind ViewModel.IsBusy, Mode=OneWay, Converter={StaticResource InverseBoolConverter}}"/>
            <HyperlinkButton Content="没有账号？去注册" HorizontalAlignment="Center"
                             Click="GoRegister_Click"/>
        </StackPanel>
    </Grid>
</Page>
